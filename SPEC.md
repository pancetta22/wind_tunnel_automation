# 風洞実験自動化システム 仕様書

**プロジェクト名**: Windy  
**作成日**: 2026-05-21  
**ステータス**: ドラフト v1.0

---

## 1. 目的

現在、実験員が手動で行っている風洞実験の計測・記録作業を完全自動化する。

自動化の対象は以下の3つである。

- 迎角ステージの制御（角度移動）
- 6軸力覚センサの計測とCSVファイルへの命名・保存
- デジタルマルチメータ（差圧電圧）の値の読み取りと記録

自動化によって生成されるファイルは、既存の後処理Python（CLα算出等）がそのまま使用できる形式に準拠すること。

---

## 2. スコープ

### 対象（本仕様書で定義する）

- 実験全体のシーケンス制御（迎角変更・計測・保存）
- 6軸CSVファイルの生成（メーカー製データロガーの代替）
- デジボルCSVファイルの生成（手書きExcelシートの代替）
- ファイル命名の自動化
- 計測中のリアルタイムモニタリング表示

### 対象外

- 風洞ブロワーの制御（操作盤による手動操作）
- デジボル電圧値から風速への変換計算（既存Pythonが担当・将来改修予定）
- CLαその他の空力データ解析（既存Pythonが担当）

---

## 3. 実験の概要

### 3.1 実験の目的

迎角 −30° 〜 +30° の各角度における翼型の空気力（揚力・抗力・モーメント）を計測する。
無風状態での計測値と有風状態での計測値の差からオフセット補正済み空気力を求める。

### 3.2 実験フェーズ

実験は以下の4フェーズを順番に実施する。

| フェーズ | 内称 | 内容 |
|---------|------|------|
| フェーズ1 | **Pofst** | 正迎角（0°〜+30°）・無風状態 |
| フェーズ2 | **Mofst** | 負迎角（0°〜−30°）・無風状態 |
| フェーズ3 | **Pdata** | 正迎角（0°〜+30°）・有風状態 |
| フェーズ4 | **Mdata** | 負迎角（0°〜−30°）・有風状態 |

フェーズ1・2（ofst）では風洞のブロワーが**停止**している。  
フェーズ3・4（data）では**ブロワーが稼働**している。  
ブロワーのON/OFFは**実験員が操作盤から手動**で行う。システムは実験員の確認入力を待ってから次の処理に進む。

### 3.3 迎角の推移

各フェーズにおける迎角の推移は以下の通り。

**Pフェーズ（正迎角）**:  
`0° → 1° → 0° → 2° → 0° → 3° → ... → 30° → 0°`

**Mフェーズ（負迎角）**:  
`0° → -1° → 0° → -2° → 0° → -3° → ... → -30° → 0°`

各フェーズの計測点数：**1 + 30×2 = 61点**

1フェーズあたりのCSVファイル数（6軸・デジボル各1ファイル/点）：**61ファイル × 2 = 122ファイル**  
4フェーズ合計：**488ファイル**

### 3.4 1計測点あたりのシーケンス

1つの計測点において、システムは以下の手順を実行する。

```
1. 迎角ステージを目標角度へ移動（移動完了まで待機）
2. 迎角到達をモニタに表示
3. angle_settle_sec 秒 待機（ロータリーステージの振動が収まるのを待つ）
4. デジボルの連続計測を開始  ─┐ 同時に開始
   6軸センサの計測を開始      ─┘ （6軸センサがファイルサイズ上限に達するまで）
5. 6軸センサのファイルサイズが force_sensor_size_limit_kb を超えた時点で
   6軸センサとデジボルの計測を同時に終了する
6. 6軸センサCSV（計測点ごと）・デジボル生データCSV（計測点ごと）を保存
   デジボルサマリーCSV（フェーズ通算）に平均値を1行追記する
7. 次の計測点へ
```

> **振動待機について**: 迎角変更直後はロータリーステージの振動が6軸センサに混入する。
> `angle_settle_sec`（デフォルト: 2秒）だけ待機してから計測を開始することでこれを回避する。
> 待機時間は `config.json` で変更可能。

> **デジボルの終了タイミングについて**: デジボルは6軸センサの計測終了と同時に打ち切る。
> サンプル数で打ち切るのではなく、6軸センサが終了するまで取り続けることで、
> 計測時間全体にわたる差圧電圧の平均を正確に求められる。

---

## 4. ファイル命名規則

### 4.1 フルファイル名（6軸CSV・デジボルCSV 共通）

```
YYYYMMDD_HHMMSS_YYMMDD_[type]_[angle].[suffix].csv
```

| フィールド | 内容 | 例 |
|-----------|------|---|
| `YYYYMMDD` | 実験日（西暦4桁） | `20260520` |
| `HHMMSS` | 計測開始時刻 | `094953` |
| `YYMMDD` | 実験日（西暦下2桁） | `260520` |
| `[type]` | フェーズ識別子（後述） | `Pofst` |
| `[angle]` | 参照迎角（2桁ゼロ埋め） | `12` |
| `[suffix]` | 計測種別（後述） | `01` |

**`[type]` の値**:

| 値 | 意味 |
|----|------|
| `Pofst` | 正迎角・無風（フェーズ1） |
| `Mofst` | 負迎角・無風（フェーズ2） |
| `Pdata` | 正迎角・有風（フェーズ3） |
| `Mdata` | 負迎角・有風（フェーズ4） |

**`[suffix]` の値**:

| 値 | 意味 |
|----|------|
| `01` | 目標迎角での計測（例：12°での計測） |
| `00` | 0°に戻った際の計測（angle N に対するオフセット基準） |

**特殊ケース**: 各フェーズの最初の計測は 0° での計測であり、このときは `[angle]=00`, `[suffix]=00` とする。

### 4.2 命名例（Pofst フェーズ）

| 物理的な迎角 | ファイル名（タイムスタンプ省略） |
|------------|-------------------------------|
| 0°（初回） | `260520_Pofst_00.00.csv` |
| 1° | `260520_Pofst_01.01.csv` |
| 0°（1°の後） | `260520_Pofst_01.00.csv` |
| 2° | `260520_Pofst_02.01.csv` |
| 0°（2°の後） | `260520_Pofst_02.00.csv` |
| ... | ... |
| 30° | `260520_Pofst_30.01.csv` |
| 0°（30°の後） | `260520_Pofst_30.00.csv` |

### 4.3 短縮名（既存Pythonとの互換用）

既存Pythonが参照する `name` フィールドはタイムスタンプなしの短縮形を用いる。

```
YYMMDD_[type]_[angle].[suffix]
```

例: `260520_Pofst_12.01`

> **注意**: フルファイル名・短縮名ともに `[angle]` と `[suffix]` の区切りは**ピリオド**（`.`）で統一する。既存の `calc_force.py` が `endswith("00.00.csv")` 等のドット区切りで判定しているため、互換性維持のためアンダースコアは使わない。

---

## 5. 出力ファイル仕様

### 5.1 6軸センサCSV

#### ファイル構造

メーカー製データロガーの出力形式と完全に一致させること。

```
行1: CFSLGR_DataFile
行2: データ保存[個/秒],1200,,Filter,OFF
行3: 定格,30,30,30,5,5,5
行4: 経過時間[sec],Fx[N],Fy[N],Fz[N],Mx[Nm],My[Nm],Mz[Nm]
行5以降: 0.0000,<Fx>,<Fy>,<Fz>,<Mx>,<My>,<Mz>
         0.0008,...
         ...
```

#### 計測仕様

| 項目 | 値 |
|------|----|
| サンプリングレート | 1200 Hz（0.0008秒間隔） |
| 計測終了条件 | ファイルサイズが **1000 KB を超えた** 時点で終了 |
| 推定計測時間 | 約20秒 |
| エンコーディング | CP932（Shift-JIS）※メーカー形式に準拠 |

#### 数値フォーマット

基本は既存CSVの桁数に準拠する。`config.json` の `csv_decimal_places` で変更可能（オプション）。

| 列 | デフォルト桁数 |
|----|--------------|
| 経過時間[sec] | `0.0000`（小数4桁） |
| Fx, Fy, Fz [N] | `0.006`（小数3桁） |
| Mx, My, Mz [Nm] | `0.0010`（小数4桁） |

> **注**: `csv_decimal_places` を省略した場合は上記のデフォルト桁数で出力する。
> グラフ表示の小数点桁数も同設定に連動する。

### 5.2 デジボルCSV

デジボルのデータは**2種類のCSV**として出力する。

---

#### (a) デジボルサマリーCSV（フェーズごと・1ファイル）

手書きExcelシートの代替。各計測点の平均値を1行ずつ蓄積し、フェーズ完了時に1ファイルとなる。

**ファイル構造**:

```
No.,迎角,name,差圧電圧[mV],風速[m/s]
0,0,260520_Pofst_00.00,-3.3,
1,1,260520_Pofst_01.01,-3.8,
2,0,260520_Pofst_01.00,-3.3,
...
```

**列仕様**:

| 列名 | 型 | 内容 |
|------|----|------|
| `No.` | 整数 | 通し番号（0始まり） |
| `迎角` | 整数 | 実際の迎角（°）。負角は負の整数 |
| `name` | 文字列 | 短縮名（`YYMMDD_type_angle.suffix`） |
| `差圧電圧[mV]` | 浮動小数点 | 計測点全サンプルの**平均値** |
| `風速[m/s]` | 空欄 | 既存Pythonが差圧電圧から計算して埋める（将来的にはExcel関数相当の式を適用する予定） |

**ファイル命名規則**:

```
YYYYMMDD_[type]_volt_summary.csv
```

| フェーズ | ファイル名例 |
|---------|------------|
| Pofst | `20260520_Pofst_volt_summary.csv` |
| Mofst | `20260520_Mofst_volt_summary.csv` |
| Pdata | `20260520_Pdata_volt_summary.csv` |
| Mdata | `20260520_Mdata_volt_summary.csv` |

計測点が完了するたびに1行ずつ追記する（フェーズ完了まで待たない）。

---

#### (b) デジボル生データCSV（計測点ごと・6軸CSVと1対1対応）

計測点ごとの全サンプルを時系列で記録する。後から計測品質の確認や再集計が可能。

**ファイル構造**:

```
sample_no,voltage_mV
0,-3.3
1,-3.4
2,-3.2
...
```

**ファイル命名規則**:

6軸センサCSVのファイル名の末尾 `.csv` を `_volt_raw.csv` に置き換えた名前とする。

```
YYYYMMDD_HHMMSS_YYMMDD_[type]_[angle].[suffix]_volt_raw.csv
```

例：`20260520_094953_260520_Pofst_12.01_volt_raw.csv`

これにより、6軸CSVとデジボル生データCSVが `name` を介して1対1で対応づけられる。

---

## 6. システム動作仕様

### 6.1 起動時の動作

1. `config.json` を読み込む
2. 迎角ステージに接続し、`homeReturn()` を実行（機械原点確定・迎角0°へ移動）
3. Leptrino センサの接続を確認
4. R6441B の接続を確認
5. 実験日時を取得し、全ファイル名の `YYYYMMDD_HHMMSS` 部分を決定
6. フェーズの選択を実験員に確認する（Pofst / Mofst / Pdata / Mdata）

### 6.2 フェーズ開始前の確認

ofst（無風）フェーズ開始前:
```
>> ブロワーが停止していることを確認してください。確認できたら Enter を押してください。
```

data（有風）フェーズ開始前:
```
>> ブロワーを起動し、風速が安定したことを確認してください。確認できたら Enter を押してください。
```

### 6.3 各計測点の動作詳細

```
[迎角ステージ] 目標角度へ移動中...
[迎角ステージ] 迎角 +12° に到達

[待機] 振動収束待ち... 2.0 秒

[計測開始] 6軸センサ & デジボル 同時計測中...
  6軸センサ: 0.0 KB / 1000.0 KB
  デジボル  : 0 サンプル取得済み
  ...
[計測完了] 6軸センサ: 1004.2 KB, デジボル: 1463 サンプル（平均: -3.81 mV）

[保存] 20260520_094953_260520_Pofst_12.01.csv         （6軸センサ）
[保存] 20260520_094953_260520_Pofst_12.01_volt_raw.csv （デジボル生データ）
[更新] 20260520_Pofst_volt_summary.csv に1行追記（23/61 点完了）
```

### 6.4 異常時の動作

| 異常 | 動作 |
|------|------|
| 迎角ステージ タイムアウト | 警告表示、実験員の確認後にリトライまたは中断を選択 |
| 6軸センサ 通信エラー | 警告表示、該当ファイルをスキップして継続するか中断を選択 |
| R6441B タイムアウト | 警告表示、デジボル値を空欄にして継続するか中断を選択 |
| 途中中断（Ctrl+C など） | それまでの計測済みデータを保存した上で終了する |

---

## 7. config.json への追加項目

本システムの実装にあたり、以下の項目を `config.json.example` に追加する。

```json
{
  "python_exe":                  "C:/Users/.../Python312-32/python.exe",
  "leptrino_port":               5,
  "qt_adl1_port":                "COM7",
  "r6441b_port":                 "COM6",
  "r6441b_timeout_sec":          5,

  "output_dir":                  "C:/Users/.../WindyData",
  "force_sensor_size_limit_kb":  1000,
  "angle_settle_sec":            2.0,
  "csv_decimal_places":          null
}
```

| 追加キー | 内容 | デフォルト |
|---------|------|-----------|
| `output_dir` | 計測CSVファイルの保存先フォルダ | （必須） |
| `force_sensor_size_limit_kb` | 6軸CSV計測終了のファイルサイズ閾値 [KB] | `1000` |
| `angle_settle_sec` | 迎角到達後、計測開始までの振動収束待ち時間 [秒] | `2.0` |
| `csv_decimal_places` | 6軸CSVとグラフの小数点桁数。`null` で既存CSV形式に準拠 | `null` |

> `r6441b_n_samples` は廃止する。デジボルのサンプル数はサンプル数上限ではなく6軸センサの終了に連動するため、config での指定は不要。

---

## 8. 技術的制約・留意事項

### 8.1 6軸センサのサンプリングレート

現在の `leptrino_server.py` は約200Hzで1秒間のデータを収集して**平均値を返す**実装になっている。
本仕様では**1200Hzで時系列データを連続取得してCSVに書き出す**必要があるため、大幅な改修が必要。

具体的には以下が必要になる。

- `GetSerialData` を1200Hzで呼び出す、または `GetLatestData` を高頻度でポーリングする方式への変更
- 経過時間・各軸データをリアルタイムでファイルに書き出す実装
- ファイルサイズを監視し、1000KB超で計測を終了するロジック

### 8.2 デジボルと6軸センサの同時計測

両センサは**同時に計測を開始**する（6軸センサが~20秒かかる間、デジボルも並行して取得）。
MATLABでの実装では `parfeval` または別プロセス起動を検討すること。

### 8.3 エンコーディング

6軸CSVはメーカー形式に合わせて **CP932（Shift-JIS）** で書き出す。
デジボルCSVは **UTF-8** で書き出す（既存PythonのCSV読み込み仕様に要確認）。

### 8.4 ファイルの保存タイミング

計測が完了した時点で即座に保存する。
途中で中断が発生した場合も、完了済みの計測点のデータは失わないこと。

---

## 9. 既存システムとの互換性まとめ

| 項目 | 既存 | 新システム |
|------|------|-----------|
| 6軸CSVの形式 | メーカーデータロガーが生成 | 本システムが同一形式で生成 |
| 6軸CSVのファイル名 | 実験員が手動入力 | 本システムが自動生成（ドット区切り `[angle].[suffix].csv` を維持） |
| dataフォルダのファイル数 | 計測点ごとに生CSV + fc10HzフィルタCSV の 2ファイル | 生CSVのみ 1ファイル |
| デジボル記録 | 目視でExcelシートに記入 | 本システムがCSVに自動記録（サマリー＋生データ） |
| デジボルCSVの列 | `No.`, `迎角`, `name`, `差圧電圧[mV]`, `風速[m/s]` | 同一 |
| 風速[m/s] 計算 | 既存Pythonが差圧電圧から計算 | 同左（将来：差圧電圧を変数とする数式で直接算出する方式に改修予定） |
| CLα 解析 | 既存Python | 既存Python（下記の軽微な修正が必要） |

### `calc_force.py` に必要な軽微な修正

本システム導入後は dataフォルダ内のファイルが **1ファイル/計測点**（fc10Hz版なし）になる。
既存の `average()` 関数は `case_num = int(len(folder_list) / 2)` で2ファイル/計測点を前提としているため、以下の修正が必要。

```python
# 修正前
case_num = int(len(folder_list) / 2)
for i in range(case_num):
    data1_name = folder_list[2 * i]   # 偶数インデックスのみ処理

# 修正後
case_num = len(folder_list)
for i in range(case_num):
    data1_name = folder_list[i]       # 全ファイルを処理
```

---

## 10. モジュール構成

本システムの実装にあたり、以下のファイルを新規作成・改修する。

> **注（2026-06）**: 以下は設計時の構成。実装後にフォルダ整理を行い、現在は
> ヘルパ類を `measurement_control/`、診断ツールを `diagnostics/` に配置している。
> **現在のディレクトリ構成は README.md を参照。**

```
Windy/
├── run_experiment.m                ★ 新規  メイン実験スクリプト
├── LeptrinoLogger.m                ★ 新規  6軸センサ 時系列ロガークラス
├── WindyMonitor.m                  ★ 新規  リアルタイムモニタリング表示クラス
├── make_filename.m                 ★ 新規  ファイル名生成ユーティリティ関数
├── get_voltage.m                   △ 改修済 config.json対応済み（追加改修不要）
├── get_sensor_data.m               △ 改修  平均値取得から時系列取得へ変更
├── QT_ADL1.m                       ○ 既存  変更なし
├── config.json.example             △ 改修  output_dir / angle_settle_sec 等を追加
└── leptrino/
    └── leptrino_server.py          △ 改修  時系列データ出力モードを追加
```

### 10.1 `run_experiment.m`（新規）

実験全体のシーケンスを制御するメインスクリプト。

**責務**:
- config.json の読み込みと各機器への接続
- フェーズ選択と実験員への確認プロンプト
- 計測点ループの制御（迎角移動 → 振動待機 → 同時計測 → 保存）
- デジボルサマリーCSVへの逐次追記
- `WindyMonitor` への状態更新
- 異常時のリトライ・中断処理

### 10.2 `LeptrinoLogger.m`（新規）

6軸センサからの時系列データ取得とCSV書き出しを担うクラス。`handle` を継承する。

**責務**:
- `leptrino_server.py` を時系列モードで呼び出す
- メーカー形式のCSVヘッダ（4行）を書き出す
- ファイルサイズを監視し、閾値超過で計測終了
- CP932エンコーディングでファイルを書き出す
- 最新サンプル値を `WindyMonitor` へリアルタイムに提供する

**主なメソッド**:

| メソッド | 説明 |
|---------|------|
| `start(filepath)` | 指定パスへのCSV書き出し開始 |
| `stop()` | 計測終了・ファイルをクローズ |
| `getSizeKB()` | 現在のファイルサイズ [KB] を返す |
| `isDone()` | ファイルサイズが閾値を超えたか判定 |
| `getLatest()` | 最新の [Fx,Fy,Fz,Mx,My,Mz] を返す（モニタ用） |

### 10.3 `WindyMonitor.m`（新規）

MATLABのFigureウィンドウを使ったリアルタイムモニタリング表示クラス。`handle` を継承する。

**表示内容**:

| パネル | 内容 |
|-------|------|
| 迎角表示 | 現在の迎角（大きく数字で表示）・フェーズ名 |
| 6軸センサグラフ | Fx/Fy/Fz/Mx/My/Mz の時系列グラフ（計測点ごとにリセット） |
| デジボル表示 | 現在のサンプル値と計測中サンプルの平均値 |
| プログレスバー | フェーズ全体の進捗（例：23/61 点完了）と計測中ファイルサイズ |

**主なメソッド**:

| メソッド | 説明 |
|---------|------|
| `update(angle, sensorData, voltage, progress)` | 全パネルを一括更新 |
| `resetGraph()` | 計測点切り替え時に6軸グラフをクリア |
| `setPhase(phase)` | フェーズ名を更新 |
| `close()` | Figureを閉じる |

> **実装上の注意**: `drawnow limitrate` を用いてグラフ更新の過負荷を防ぐこと。
> 計測ループのブロッキングを最小限に抑えるため、更新頻度は計測点ループ内で制御する。

### 10.4 `make_filename.m`（新規）

ファイル名文字列を生成するユーティリティ関数。

```matlab
% フルファイル名（6軸CSV用）
fname = make_filename(date_str, time_str, phase, ref_angle, suffix, 'full');
% → '20260520_094953_260520_Pofst_12.01.csv'

% デジボル生データCSV用
fname = make_filename(date_str, time_str, phase, ref_angle, suffix, 'volt_raw');
% → '20260520_094953_260520_Pofst_12.01_volt_raw.csv'

% 短縮名（デジボルサマリーCSVのname列用）
name = make_filename(date_str, time_str, phase, ref_angle, suffix, 'short');
% → '260520_Pofst_12.01'

% デジボルサマリーCSVファイル名
fname = make_filename(date_str, '', phase, 0, 0, 'volt_summary');
% → '20260520_Pofst_volt_summary.csv'
```

### 10.5 `leptrino_server.py` の改修

現在は「1秒間の平均値を返す」実装になっている。以下の2モードを持つように改修する。

| モード | 起動オプション | 動作 |
|--------|--------------|------|
| 平均モード（既存） | `--mode avg` | 現在の動作と同じ。平均値をJSONで返す |
| 時系列モード（新規） | `--mode stream --output <path> --size_limit_kb <KB>` | 指定パスにCSVを書き出し続ける。ファイルサイズ上限超過で自動終了 |

時系列モードでのCSV書き出し仕様:
- ヘッダ4行はPython側で書き出す
- データ行は `0.0000,Fx,Fy,Fz,Mx,My,Mz` の形式
- バッファリングを最小化し、書き出しの遅延を抑える（`flush` を各行後に呼ぶ）
- ファイルサイズが `--size_limit_kb` を超えたら自動終了

---

## 11. 実装タスク一覧

開発の優先順位を考慮した実装順序を示す。

### フェーズA：基盤整備（前提となる改修）

| # | タスク | 対象ファイル | 優先度 |
|---|--------|------------|--------|
| A-1 | `leptrino_server.py` に時系列モードを追加 | `leptrino/leptrino_server.py` | 高 |
| A-2 | `make_filename.m` の実装（full / volt_raw / short / volt_summary の4モード） | `make_filename.m` | 高 |
| A-3 | `config.json.example` に新キーを追加（`output_dir`, `force_sensor_size_limit_kb`, `angle_settle_sec`, `csv_decimal_places`） | `config.json.example` | 高 |

### フェーズB：コアロジック実装

| # | タスク | 対象ファイル | 優先度 |
|---|--------|------------|--------|
| B-1 | `LeptrinoLogger.m` の実装 | `LeptrinoLogger.m` | 高 |
| B-2 | `run_experiment.m` の実装（振動待機・同時計測・同時終了を含むループ） | `run_experiment.m` | 高 |
| B-3 | デジボルサマリーCSVへの逐次追記ロジック | `run_experiment.m` 内 | 高 |
| B-4 | デジボル生データCSVの保存ロジック | `run_experiment.m` 内 | 高 |

### フェーズC：モニタリング実装

| # | タスク | 対象ファイル | 優先度 |
|---|--------|------------|--------|
| C-1 | `WindyMonitor.m` の実装（Figureレイアウト・各パネル） | `WindyMonitor.m` | 中 |
| C-2 | `LeptrinoLogger` から `WindyMonitor` へのリアルタイムデータ供給 | `LeptrinoLogger.m` | 中 |
| C-3 | `run_experiment.m` から `WindyMonitor.update()` の呼び出し | `run_experiment.m` | 中 |

### フェーズD：UX・堅牢化

| # | タスク | 対象ファイル | 優先度 |
|---|--------|------------|--------|
| D-1 | フェーズ選択・確認プロンプトの実装 | `run_experiment.m` | 中 |
| D-2 | 異常時リトライ・中断処理の実装 | `run_experiment.m` | 中 |
| D-3 | 途中再開機能（中断した計測点から再開） | `run_experiment.m` | 低 |

### フェーズE：ドキュメント整備

| # | タスク | 対象ファイル | 優先度 |
|---|--------|------------|--------|
| E-1 | `CLAUDE.md` の更新（新ファイル・改修ルールを反映） | `CLAUDE.md` | 中 |
| E-2 | `README.md` の更新（メイン実験スクリプトの使い方を追記） | `README.md` | 中 |
| E-3 | `SPEC.md` のステータスを `v1.0 確定` に更新 | `SPEC.md` | 低 |

---

## 12. 未決事項（要確認）

実装開始前に以下の点を確認・決定すること。

| # | 項目 | 現状 | 確認が必要な理由 |
|---|------|------|----------------|
| 1 | デジボルCSVのエンコーディング | UTF-8（確定） | 既存 `calc_force.py` は `windspeed.csv` を `pd.read_csv()` でエンコーディング指定なしに読んでいるため UTF-8 で問題なし |
| 2 | 6軸センサの実効サンプリングレート | 1200Hzと仮定 | `GetSerialData` を1200Hzで安定して呼べるか実機で確認が必要 |
| 3 | デジボルと6軸センサの同時計測方法 | `parfeval` を想定 | MATLABのParallel Computing Toolbox の有無に依存する。なければ代替手段（`system` による非同期プロセス起動）を検討 |
      結論：Parallel Computing Toolboxなし
| 4 | 途中再開機能の要否 | 低優先度として保留 | 1フェーズあたり約20分×4フェーズ≒80分の実験。中断リスクの許容度による |
| 5 | `output_dir` 以下のフォルダ構成 | 未定 | 実験日ごとにサブフォルダを切るか、フラットに並べるか |
| 6 | `WindyMonitor` のグラフ更新頻度 | 未定 | 高頻度更新（1200Hz）は描画負荷が高い。間引き頻度（例：10Hz更新）を決める必要がある |
| 7 | `angle_settle_sec` のデフォルト値 | 2.0秒と仮定 | 実機でロータリーステージの振動収束時間を計測して決定することが望ましい |
